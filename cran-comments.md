## R CMD check results

0 errors | 0 warnings | 0 notes

* This is a new submission.
* Checked on Windows (local R 4.6.0), Windows win-builder (devel and release).
* The `get_gaia_data()` function makes internet requests; its example is
  wrapped in `\dontrun{}`.

